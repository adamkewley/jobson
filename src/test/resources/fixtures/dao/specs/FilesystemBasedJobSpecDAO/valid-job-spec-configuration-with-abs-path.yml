name: A valid job spec
name: An example of a valid job spec

expectedInputs:
- id: name
  type: select
  name: The user's name
  name: The name of the user
  options:
  - id: adam
    name: adam
  - id: patrick
    name: patrick

execution:
  application: echo
  arguments:
    - Hello world
  dependencies:
    - source: /tmp/libyaml.so
      target: libyaml.so