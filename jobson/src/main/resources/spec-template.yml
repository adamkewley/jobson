name: Generated job spec

description: >

  A job spec generated by jobson. The admin should've changed this.


expectedInputs:  # Optional

- id: string-example
  type: string
  name: Example String Input
  description: Expects clients to provide a singular value  # Optional
  default: Some default                                     # Optional


- id: stringarray-example
  type: string[]
  name: Example Multivalue Input
  description: Expects clients to provide an array of values  # Optional
  default:                                                    # Optional
  - val1
  - val2


- id: select-example
  type: select
  name: Example Select Input
  description: Expects clients to provide one ID from the list of options  # Optional

  options:
  - id: opt1
    name: First Option
  - id: opt2
    name: Second Option
    description: The second option   # Optional

  default: opt1                      # Optional


- id: sql-example
  type: sql
  name: SQL Input
  description: Expects clients to build a well-formed SQL query against the tables  # Optional

  tables:
  - id: table1
    name: Table 1
    description: The first table
    columns:
    - id: column1
      name: Column 1
      description: The first column
      type: string
    - id: column2
      name: Column 2
      description: The second column
      type: int


execution:

  application: echo

  arguments:  # Optional
  - ${toJSON(request)}
#  - a non-special string arg
#  - ${toFile(toJSON(request))}
#  - ${inputs["string-example"]}
#  - ${toJSON(inputs["sql-example"])}
#  - A combination of text and templates ${inputs["string-example"]}
#
#  dependencies:
#    source: some-file-in-spec-dir
#    target: some-path-in-working-dir


# expectedOutputs:
#
# - id: id-as-downloaded-by-client.zip
#   path: path-written-by-runtime
#   mimeType: application/octet-stream      # optional (autodetected otherwise)
#   name: Name of the output                # optional
#   description: Description of the output  # optional
#
# - id: ${request.id}_results.zip
#   path: output.zip